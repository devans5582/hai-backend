# Required — set this in your deployment platform's environment variables
# Never put the real key in this file or commit it anywhere
# IMPORTANT: do NOT wrap the value in quotes (write key=sk-abc, not key="sk-abc")
OPENAI_API_KEY=your-openai-api-key-here

# Optional — defaults to 3000 if not set
PORT=3000
